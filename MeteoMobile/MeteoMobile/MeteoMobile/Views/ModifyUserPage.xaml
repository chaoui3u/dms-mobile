<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:ViewModels="clr-namespace:MeteoMobile.ViewModels"
             x:Class="MeteoMobile.Views.ModifyUserPage">
    <ContentPage.BindingContext>
        <ViewModels:ModifyUserViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout>
            <Entry x:Name="firstName" Text="{Binding FirstName}" Placeholder="first name"/>
            <Entry x:Name="lastName"  Text="{Binding LastName}" Placeholder="last name"/>
            <Entry x:Name="password"  Text="{Binding Password}" Placeholder="password" IsPassword="True"/>
            <Entry x:Name="confirmPassword" Text="{Binding ConfirmPassword}" Placeholder="password confirmation" IsPassword="True"/>
            <Entry x:Name="email" Text="{Binding Email}" Placeholder="email"/>
            <Picker x:Name="rolePicker" Title="Selectionner un role" SelectedItem="{Binding Role}">
                <Picker.Items>
                    <x:String>Admin</x:String>
                    <x:String>Staff</x:String>
                </Picker.Items>
            </Picker>

            <Button Command="{Binding ModifyUserCommand}" Text="Modifier" Clicked="Button_Clicked"/>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>